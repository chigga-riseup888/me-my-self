# M8280 / RG258UB_GLAB / UIS8510 / v610 完整刷机指南

> BBA 3.3.0 · Linux 6.6 · 1H10 NAND · 256 MiB SPI NAND

## 0. 文档范围与工程基线

本文描述当前 M8280 / RG258UB_GLAB / UIS8510 / v610 工程的全量 PAC 构建与烧录实现，重点覆盖两部分：

1. PAC 的组成、物料来源、签名与打包过程；
2. QFlash 从正常 AT 口进入下载模式，到 FDL/FDL2 执行重分区、擦除、写入、Reset 的完整烧录流程。

烧录后的正常启动链仅保留与全量刷机闭环直接相关的内容。NV Group、主备写入、坏块、`log` autoresize、Verify 等机制集中在“补充机制与边界”章节。

### 0.1 当前工程基线

| 项目 | 当前值 |
|---|---|
| MODEL / SPEC | `M8280 / EU` |
| Quectel Project | `RG258UB_GLAB` |
| Revision | `RG258UB_GLABR01A01M4G` |
| SoC / Platform | `UIS8510 / v610` |
| Board | `1H10 NAND` |
| Kernel | Linux 6.6 |
| Flash | 256 MiB SPI NAND |
| 正式工程根目录 | `/home/bba/work/bba/610/bba/3.3.0/bba_3_0_platform` |
| SDK Gold HEAD | `fc1b1da38c0b2ea9b327a3c8b7f744c2a96df4f0` |

### 0.2 证据范围

本文使用的当前证据分为三个现场：

- **PAC 构建现场**：2026-09-08 当前 BBA 3.3.0 构建产物；
- **全量烧录现场**：2026-09-07 QFlash 成功烧录日志 + DBG UART；
- **FDL2 实现**：当前 SDK HEAD 下的 `uboot44` dloader / NAND / UBI 源码。

当前板端后续状态异常，因此本文的 Runtime 与启动结论以 2026-09-07 成功烧录日志为准，不使用当前异常状态替代成功现场。

### 0.3 当前 FDL2 NAND 配置

当前目标配置文件：

```text
sdk/ql_rg620ua/unisoc/uboot44/configs/uis8510_1h10_nand_defconfig
```

已确认启用：

```text
CONFIG_NAND_BOOT=y
CONFIG_DM_MTD=y
CONFIG_MTD=y
CONFIG_MTD_UBI=y
CONFIG_UBI_ATTACH_MTD=y
CONFIG_NV_SUPPORT_GROUP=y
CONFIG_NV_SUPPORT_COMPRESS=y
```

---

# 1. PAC 组成与构建

## 1.1 当前最终 PAC

当前 BBA 最终 PAC：

```text
platform/targets/M8280/EU/image/
uis8510_1h10_nand+tiny+console+sec+cpe-user-native_UIS8510_1H10_SEC.pac
```

当前构建信息：

| 项目 | 值 |
|---|---:|
| 文件大小 | `89,365,802 B` |
| MD5 | `32908ccb6e26669f86148afc2599a139` |
| Secure Variant | `UIS8510_1H10_SEC` |
| PAC Product | `uis8510-1h10-nand-cpe` |

PAC 的内容由 `pac.ini` 中的下载项、对应 payload，以及 `Partitions.xml` 的 Block / Partition 描述共同确定。

---

## 1.2 Flash 存储结构

当前 256 MiB SPI NAND 分为两类目标：

1. 固定 offset / size 的 raw MTD 分区；
2. `ubipac` raw MTD 分区内部的 UBI Volume。

### 1.2.1 外层 raw MTD

| MTD | 名称 | Offset | Size | 主要用途 |
|---:|---|---:|---:|---|
| 0 | `splloader` | `0x00000000` | 512 KiB | SPL |
| 1 | `splloaderbak` | `0x00080000` | 512 KiB | SPL 备份区 |
| 2 | `sml` | `0x00100000` | 768 KiB | SML |
| 3 | `smlbak` | `0x001c0000` | 768 KiB | SML 备份区 |
| 4 | `trustos` | `0x00280000` | 4 MiB | TOS / TEE |
| 5 | `trustosbak` | `0x00680000` | 4 MiB | TOS 备份区 |
| 6 | `teecfg` | `0x00a80000` | 512 KiB | TEE 配置 |
| 7 | `teecfgbak` | `0x00b00000` | 512 KiB | TEE 配置备份区 |
| 8 | `sysdump` | `0x00b80000` | 1 MiB | Sysdump image |
| 9 | `dumpstatus` | `0x00c80000` | 256 KiB | Dump 状态 |
| 10 | `uboot` | `0x00cc0000` | 3 MiB | U-Boot |
| 11 | `ubootbak` | `0x00fc0000` | 3 MiB | U-Boot 备份区 |
| 12 | `rawdata` | `0x012c0000` | 768 KiB | 运行状态 / KV |
| 13 | `custinfo` | `0x01380000` | 768 KiB | 客户持久数据 |
| 14 | `logo` | `0x01440000` | 3 MiB | Boot Logo |
| 15 | `ubipac` | `0x01740000` | 232.75 MiB | UBI 容器 |

成功烧录日志中 raw erase 的地址步进为 `0x20000`，对应当前 NAND 128 KiB eraseblock。

### 1.2.2 `ubipac` 内 UBI Volume

当前 repartition 日志确认 26 个 user Volume：

| ID | Volume | 当前配置大小 | 主要用途 / 下载项 |
|---:|---|---:|---|
| 0 | `prodnv` | 5120 KiB | 产线 / 校准 NV |
| 1 | `project_RG258UB_GLAB` | 256 KiB | Project 信息 |
| 2 | `nr_downloadnv` | 5120 KiB | `NV_NR_G` |
| 3 | `nr_factorynv` | 3840 KiB | Group NV |
| 4 | `calinv` | 3840 KiB | Factory NV 备份 |
| 5 | `nr_runtimenv1` | 10240 KiB | Runtime NV |
| 6 | `nr_runtimenv2` | 10240 KiB | Runtime NV 备份 |
| 7 | `miscdata` | 1024 KiB | 下载 / 运行状态 |
| 8 | `misc` | 1024 KiB | Misc 数据 |
| 9 | `nr_deltanv` | 1024 KiB | `Modem_NR_DELTANV` |
| 10 | `nr_modem` | 15360 KiB | `Modem_NR` |
| 11 | `nr_phy` | 8192 KiB | `Modem_NR_PHY` |
| 12 | `l_agdsp` | 5120 KiB | `DSP_LTE_AG` |
| 13 | `pm_sys` | 512 KiB | `DFS` |
| 14 | `recovery` | 8192 KiB | `Recovery` |
| 15 | `recoveryfs` | 5120 KiB | `Recoveryfs` |
| 16 | `uboot_log` | 1024 KiB | `EraseUBOOTLOG` |
| 17 | `sysdumpdb` | 3072 KiB | `EraseSysdumpdb` |
| 18 | `boot` | 8192 KiB | `BOOT` |
| 19 | `system` | 19456 KiB | BBA RootFS / `System` |
| 20 | `rootfs_data` | 61876 KiB | `EraseRootfs_data` |
| 21 | `oem_data` | 10240 KiB | `EraseOem_data` |
| 22 | `misc_ro` | 10292 KiB / 83 LEB | `Misc_ro` |
| 23 | `misc_rw` | 10292 KiB / 83 LEB | `Misc_rw` |
| 24 | `misc_rw_bak` | 10292 KiB / 83 LEB | `Misc_rw_bak` |
| 25 | `log` | `0xFFFFFFFF` | `EraseLog` + autoresize |

成功烧录现场 UBI 几何：

```text
PEB size             = 131072 B  (128 KiB)
LEB size             = 126976 B  (124 KiB)
Min I/O              = 2048 B
Good PEBs            = 1862
Bad PEBs             = 0
Reserved for bad PEB = 40
User volumes         = 26
```

---

## 1.3 PAC 内容总表

下表以当前 final `pac.ini` 为主，列出本次 Secure CPE PAC 的实际下载项。

| 类别 | PAC Item | 当前 payload | XML Block / 目标 | 存储类型 | 处理方式 |
|---|---|---|---|---|---|
| 配置 | `CONFILE` | `uis8510-1h10-nand-cpe.xml` | 分区 / Scheme 配置 | 下载元数据 | Host 使用 |
| 下载程序 | `FDL` | `fdl1-sign.bin` | 无普通 Flash Block | 下载阶段 | Loader |
| 下载程序 | `FDL2` | `fdl2-sign.bin` | 无普通 Flash Block | 下载阶段 | NAND/UBI 下载执行环境 |
| SPL | `SPLLoader` | `u-boot-spl-16k-sign.bin` | `splloader` | raw MTD | SPL 专用 writer |
| NV | `NV_NR_G` | `nvitem.bin` | `nr_downloadnv` | UBI Volume | Group NV |
| CP | `Modem_NR` | `cp_sign/...modem-sign.bin` | `nr_modem` | UBI Volume | UBI update |
| CP | `Modem_NR_DELTANV` | `cp_sign/...deltanv-sign.bin` | `nr_deltanv` | UBI Volume | UBI update |
| CP | `Modem_NR_PHY` | `cp_sign/...PHY...-sign.bin` | `nr_phy` | UBI Volume | UBI update |
| DSP | `DSP_LTE_AG` | `cp_sign/...adsp-sign.bin` | `l_agdsp` | UBI Volume | UBI update |
| MCU/PM | `DFS` | `cp_sign/...UIS8510_SP-sign.bin` | `pm_sys` | UBI Volume | UBI update |
| Linux | `BOOT` | `boot-sign.img` | `boot` | UBI Volume | UBI update |
| Recovery | `Recovery` | `boot-sign.img` | `recovery` | UBI Volume | UBI update |
| Logo | `BootLogo` | `quectel_logo.bmp` | `logo` | raw MTD | raw erase/write |
| Secure | `SML` | `sml-sign.bin` | `sml` | raw MTD | 主/备 raw 写入 |
| Bootloader | `UBOOTLoader` | `u-boot-sign.bin` | `uboot` | raw MTD | 主/备 raw 写入 |
| Secure | `Teecfg` | `teecfg-sign.bin` | `teecfg` | raw MTD | 主/备 raw 写入 |
| Secure | `Trustos` | `tos-sign.bin` | `trustos` | raw MTD | 主/备 raw 写入 |
| RootFS | `System` | `root-sign.squashfs` | `system` | UBI Volume | UBI update |
| BBA misc | `Misc_ro` | `misc_ro.ubifs` | `misc_ro` | UBI Volume | UBI update |
| BBA misc | `Misc_rw` | `misc_rw.ubifs` | `misc_rw` | UBI Volume | UBI update |
| BBA misc | `Misc_rw_bak` | `misc_rw.ubifs` | `misc_rw_bak` | UBI Volume | UBI update |
| 诊断 | `Sysdump` | `sysdump-sign.bin` | `sysdump` | raw MTD | raw erase/write |
| Recovery | `Recoveryfs` | `recovery-sign.squashfs` | `recoveryfs` | UBI Volume | UBI update |
| MCU | `Mcufw` | `Project.bin` | `mcufw` | Host stage 已确认 | 当前 UART 未提供独立 Flash write 锚点 |

### 1.3.1 Erase / Control Item

final `pac.ini` 中同时存在无 payload 的控制项：

```text
FLASH_NR=1@
EraseUBOOT=1@
EraseUBOOTLOG=1@
EraseDumpstatus=1@
EraseSysdumpdb=1@
EraseRootfs_data=1@
EraseOem_data=1@
EraseLog=1@
EraseUserData=1@
EraseSPLLoader=1@
```

本次 QFlash 成功日志形成独立执行阶段的项目为：

```text
EraseSPLLOADER
EraseUBOOTLOG
EraseDumpstatus
EraseSysdumpdb
EraseRootfs_data
EraseOem_data
EraseLog
```

`FLASH_NR`、`EraseUBOOT`、`EraseUserData` 未在本次 QFlash 摘要中形成独立同名阶段，本文不为它们补充未观测到的独立擦写动作。

---

## 1.4 `Partitions.ini → pac.ini → XML` 映射

### 1.4.1 `Partitions.ini`

当前源文件：

```text
platform/build/model/M8280/EU/partitions/Partitions.ini
```

构建覆盖到：

```text
sdk/ql_rg620ua/unisoc/prebuilts/pac_config/uis8510-1h10-nand.ini
```

当前 MD5：

```text
9460110bf50308388ddb0ff52f030605
```

Secure CPE variant：

```ini
[UIS8510_1H10_SEC]
CONFILE=./bin/unisoc/temp/uis8510-1h10-nand-cpe.xml
FDL=1@./bin/unisoc/temp/fdl1-sign.bin
FDL2=1@./bin/unisoc/temp/fdl2-sign.bin
SPLLoader=1@./bin/unisoc/temp/u-boot-spl-16k-sign.bin
NV_NR_G=1@./bin/unisoc/temp/nvitem.bin
...
System=1@./bin/unisoc/temp/root-sign.squashfs
Misc_ro=1@./bin/unisoc/temp/misc_ro.ubifs
Misc_rw=1@./bin/unisoc/temp/misc_rw.ubifs
Misc_rw_bak=1@./bin/unisoc/temp/misc_rw.ubifs
...
```

### 1.4.2 final `pac.ini`

`build_pac()` 调用 `parser_ini.py` 生成当前 variant 的 final `pac.ini`：

```text
sdk/ql_rg620ua/bin/unisoc/temp/
cp_sign/UIS8510_1H10_SEC/pac.ini
```

`sign_cp.sh` 执行后，CP/Modem/DSP 相关条目的 payload 路径被替换为 `cp_sign/UIS8510_1H10_SEC/` 下的 signed image。

例如：

```ini
Modem_NR=1@./bin/unisoc/temp//cp_sign/UIS8510_1H10_SEC/SC9600_QogirN5_PS_V610_CPE_LIC_modem-sign.bin
Modem_NR_DELTANV=1@./bin/unisoc/temp//cp_sign/UIS8510_1H10_SEC/deltanv-sign.bin
Modem_NR_PHY=1@./bin/unisoc/temp//cp_sign/UIS8510_1H10_SEC/SC9600_QogirN5_PHY_V610_CPE_modem-sign.bin
DSP_LTE_AG=1@./bin/unisoc/temp//cp_sign/UIS8510_1H10_SEC/ums9632_v610_adsp-sign.bin
DFS=1@./bin/unisoc/temp//cp_sign/UIS8510_1H10_SEC/UIS8510_SP-sign.bin
```

### 1.4.3 `Partitions.xml`

当前 Model XML：

```text
platform/build/model/M8280/EU/partitions/Partitions.xml
```

覆盖到 SDK prebuilt XML：

```text
sdk/ql_rg620ua/unisoc/prebuilts/pac-binary/uis8510/uis8510-1h10-nand-cpe.xml
```

两者当前 MD5：

```text
e588978de2f49ff508b59241200161e1
```

构建后 temp XML：

```text
sdk/ql_rg620ua/bin/unisoc/temp/uis8510-1h10-nand-cpe.xml
```

当前 temp XML 已将：

```text
project_quectel
```

替换为：

```text
project_RG258UB_GLAB
```

XML Scheme 将 PAC Logical ID 映射到 Block，例如：

```text
ID=System      → Block=system
ID=BOOT        → Block=boot
ID=Recoveryfs  → Block=recoveryfs
ID=Misc_ro     → Block=misc_ro
ID=SML         → Block=sml
ID=UBOOTLoader → Block=uboot
```

---

## 1.5 BBA RootFS 生成

BBA 最终 RootFS 工作目录：

```text
platform/targets/M8280/EU/filesystem
```

BBA 通过：

```text
platform/build/makes/Makefile.sdk.quecopen_rg620ua.mk
```

调用 `mksquashfs4` 生成：

```text
platform/targets/M8280/EU/image/root.squashfs
```

当前主链：

```text
SDK package / staging runtime
    ↓
supplier_fs_bin
    ↓
BBA filesystem
platform/targets/M8280/EU/filesystem
    ↓
BBA app / module / model overlay
    ↓
mksquashfs4
    ↓
platform/targets/M8280/EU/image/root.squashfs
```

`build_fw_pack()` 使用这个 BBA `root.squashfs` 作为 external rootfs，而不是直接使用 SDK Demo rootfs。

---

## 1.6 `build_fw_pack()` 物料汇聚

当前 BBA packaging 配置源：

```text
platform/build/model/M8280/EU/exfiles/ql_build_config
```

构建执行副本：

```text
sdk/ql_rg620ua/quectel/unisoc/compile/ql_build_config
```

当前 MD5：

```text
8121b26449d64a2936b5bd4d5cc6262
```

`build_fw_pack()` 将整机物料汇聚到：

```text
sdk/ql_rg620ua/bin/unisoc/temp/
```

当前 temp 主要原始物料：

```text
fdl1.bin
fdl2.bin
u-boot-spl-16k.bin
u-boot.bin
sml.bin
teecfg.bin
tos.bin
sysdump.bin
boot.img
root.squashfs
recovery.squashfs
nvitem.bin
deltanv.bin
UIS8510_SP.bin
ums9632_v610_adsp.bin
Project.bin
misc_ro.ubifs
misc_rw.ubifs
quectel_logo.bmp
uis8510-1h10-nand-cpe.xml
```

当前关键尺寸：

| 文件 | 大小 |
|---|---:|
| `fdl1.bin` | 126,463 B |
| `fdl1-sign.bin` | 212,664 B |
| `fdl2.bin` | 1,115,144 B |
| `fdl2-sign.bin` | 1,116,872 B |
| `u-boot.bin` | 1,115,144 B |
| `u-boot-sign.bin` | 1,116,872 B |
| `sml.bin` | 311,296 B |
| `sml-sign.bin` | 312,984 B |
| `teecfg-sign.bin` | 124,568 B |
| `tos-sign.bin` | 2,482,392 B |
| `sysdump-sign.bin` | 617,720 B |
| `boot.img` | 5,969,920 B |
| `boot-sign.img` | 5,971,608 B |
| `root.squashfs` | 16,807,232 B |
| `root-sign.squashfs` | 16,808,920 B |
| `recovery.squashfs` | 4,513,584 B |
| `recovery-sign.squashfs` | 4,515,272 B |
| `misc_ro.ubifs` | 1,650,688 B |
| `misc_rw.ubifs` | 1,650,688 B |
| `nvitem.bin` | 24,565,836 B |
| `quectel_logo.bmp` | 2,764,856 B |

---

## 1.7 AP / Boot 签名

`build_fw_pack()` 对下列 AP / Boot 物料调用 `build_sign()`：

```text
sml.bin
fdl1.bin
u-boot-spl-16k.bin
fdl2.bin
u-boot.bin
boot.img
teecfg.bin
tos.bin
sysdump.bin
root.squashfs
recovery.squashfs
```

当前输出关系：

```text
sml.bin              → sml-sign.bin
fdl1.bin             → fdl1-sign.bin
u-boot-spl-16k.bin   → u-boot-spl-16k-sign.bin
fdl2.bin             → fdl2-sign.bin
u-boot.bin           → u-boot-sign.bin
boot.img             → boot-sign.img
teecfg.bin           → teecfg-sign.bin
tos.bin              → tos-sign.bin
sysdump.bin           → sysdump-sign.bin
root.squashfs        → root-sign.squashfs
recovery.squashfs    → recovery-sign.squashfs
```

当前签名流程由 `ql_build_config::build_sign()` 调用 SecureBoot 工具生成 signed image。

---

## 1.8 CP / Modem 签名

`build_pac()` 调用：

```text
sdk/ql_rg620ua/unisoc/prebuilts/scripts/packimage_scripts/sign_cp.sh
```

处理 Modem / PHY / ADSP / PM 等 CP 侧 payload，并将 final `pac.ini` 的路径更新到：

```text
sdk/ql_rg620ua/bin/unisoc/temp/cp_sign/UIS8510_1H10_SEC/
```

当前 final `pac.ini` 已确认使用 signed CP image。

---

## 1.9 PAC 最终生成链

当前 `ql_build_config` 的实际调用顺序：

```text
BBA root.squashfs
    ↓
build_fw_pack(external_rootfs)
    ↓
bin/unisoc/temp 物料汇聚
    ↓
build_sign()
    ↓
build_pac()
    ↓
parser_ini.py
    ↓
cp_sign/UIS8510_1H10_SEC/pac.ini
    ↓
sign_cp.sh
    ↓
update_nv_pac_config.sh        [脚本存在时执行]
    ↓
binary_gzip
    ↓
makepac.py
    ↓
最终 *.pac
    ↓
platform/targets/M8280/EU/image/
```

主要源码 / 脚本位置：

| 职责 | 路径 / 函数 |
|---|---|
| BBA image 入口 | `platform/build/makes/Makefile.sdk.quecopen_rg620ua.mk` |
| Packaging 配置源 | `platform/build/model/M8280/EU/exfiles/ql_build_config` |
| Packaging 执行副本 | `sdk/ql_rg620ua/quectel/unisoc/compile/ql_build_config` |
| 固件汇聚 | `build_fw_pack()` |
| AP / Boot 签名 | `build_sign()` |
| PAC 生成入口 | `build_pac()` |
| INI 解析 | `parser_ini.py` |
| CP 签名 | `sign_cp.sh` |
| Binary 压缩 | `binary_gzip()` |
| PAC 打包 | `makepac.py` |

---

# 2. 全量烧录流程

## 2.1 本次烧录输入

本章基于 2026-09-07 成功烧录现场。

输入 PAC 对应当前 M8280 / RG258UB_GLAB / UIS8510 Secure CPE 固件。QFlash 保存的主日志从端口查找开始，因此 Host 内部“打开 PAC / 解析 PAC 容器”的闭源步骤不在当前日志阶段中展开；PAC 内容及映射由第 1 章的 final `pac.ini` / XML / build 产物确认。

烧录主时间：

```text
15:22:34  开始查找 AT Port
15:22:51  DL Start / FDL
15:22:52  FDL2
15:22:58  Integrity Check / Repartition
15:22:59  开始实际 Erase / Download
15:23:41  SPLLoader / Reset / Download Successful
15:23:51  FW upgrade success
```

---

## 2.2 步骤 1：QFlash 查找正常 AT Port

QFlash 枚举当前 Windows 串口并锁定：

```text
COM89
Quectel USB AT Port
```

日志：

```text
Port(89) find...
Wait port(89) is normal...
Get Port Pass
Port(89) find success...
Port(89) check QDLoader...
COM89
AT 89
Port(89) is AT port
```

本阶段设备仍处于正常系统 USB 组合，QFlash 使用 AT Port 作为进入下载模式的控制入口。

Flash 变化：无。

---

## 2.3 步骤 2：QFlash 通过 AT Port 下发下载模式命令

QFlash 打开 COM89 后发送：

```text
AT+QDOWNLOAD=1
```

日志：

```text
Switch EDL mode(Port89) ......
Open Port89 Pass
at+qdownload=1
Switch Download Port success...
```

设备收到命令后开始从正常 USB 功能切换到 Unisoc 下载端口。

Flash 变化：无。

---

## 2.4 步骤 3：正常 AT Port 消失

QFlash持续检查原 AT Port：

```text
Wait AT Port normal exist...
AT Port not exist[PASS]
```

随后重新枚举 Windows 串口。

Flash 变化：无。

---

## 2.5 步骤 4：设备枚举为 SPRD U2S DIAG / QDLoader

QFlash 在重新枚举中检测到：

```text
SPRD U2S DIAG
COM88
```

日志：

```text
SPRD U2S DIAG
Ch1:Qdloader 1: 88
Ch1:New Qdloader 1: 88
Wait port(88) is normal...
Get Port Pass
Switch EDL mode completely
QDownload Success(88)
AT(89)->QDownload(88)
```

当前下载会话的通信端口由：

```text
COM89 Quectel USB AT Port
```

切换到：

```text
COM88 SPRD U2S DIAG
```

Flash 变化：无。

---

## 2.6 步骤 5：QFlash 启动下载会话

日志：

```text
DL Start
```

QFlash开始向设备下载 FDL 阶段程序。

Flash 变化：无普通固件分区写入。

---

## 2.7 步骤 6：下载 FDL1

QFlash：

```text
15:22:51 FDL
```

当前 PAC：

```ini
FDL=1@./bin/unisoc/temp/fdl1-sign.bin
```

FDL1 文件：

```text
sdk/ql_rg620ua/bin/unisoc/temp/fdl1-sign.bin
```

FDL1属于下载阶段执行程序，不作为普通系统运行分区镜像处理。

Flash 变化：无普通业务分区写入。

---

## 2.8 步骤 7：下载 FDL2

QFlash：

```text
15:22:52 FDL2
```

当前 PAC：

```ini
FDL2=1@./bin/unisoc/temp/fdl2-sign.bin
```

FDL2 文件：

```text
sdk/ql_rg620ua/bin/unisoc/temp/fdl2-sign.bin
```

FDL2进入下载命令处理后执行：

```text
U_BOOT_CMD("download")
    ↓
do_download()
    ↓
dl_prepare_init()
    ↓
dl_packet_init()
    ↓
fdl_ubi_dev_init()       [CONFIG_NAND_BOOT]
    ↓
dl_register_func()
    ↓
dl_ackpkt_init()
    ↓
dl_cmd_handler()
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/cmd_download.c
```

`dl_register_func()` 注册当前主要 Host command handler：

| Command | Handler | 作用 |
|---|---|---|
| `BSL_CMD_START_DATA` | `dl_cmd_write_start()` | 建立一个 payload 下载 |
| `BSL_CMD_MIDST_DATA` | `dl_cmd_write_midst()` | 接收并写入中间数据 |
| `BSL_CMD_END_DATA` | `dl_cmd_write_end()` | 完成 payload 下载 |
| `BSL_ERASE_FLASH` | `dl_cmd_erase()` | 擦除目标 |
| `BSL_REPARTITION` | `dl_cmd_repartition()` | 处理 UBI Volume repartition |
| `BSL_CMD_NORMAL_RESET` | `dl_cmd_reboot()` | 下载结束 Reset |

Flash 变化：FDL2 初始化时 attach 当前 `ubipac`，尚未进入本次批量镜像写入。

---

## 2.9 步骤 8：NV 备份阶段 `_BKF_NV_NR_G`

QFlash：

```text
15:22:55 _BKF_NV_NR_G
```

同一时段设备侧日志访问：

```text
nr_downloadnv
nr_factorynv
nr_runtimenv1
nr_runtimenv2
```

当前 final `pac.ini`：

```text
NvBackup=1
```

设备侧 current target 使用 Group NV，后续 `NV_NR_G` 下载会处理：

```text
nr_downloadnv
nr_factorynv
calinv
nr_runtimenv1
nr_runtimenv2
```

Host 侧 `NvBackup=1` 的完整内部策略未开放，本文记录 QFlash 的 `_BKF_NV_NR_G` 阶段与设备侧已确认的 NV Group 行为。

---

## 2.10 步骤 9：完整性检查 `_INTEGRITY_CHECK_`

QFlash：

```text
15:22:58 _INTEGRITY_CHECK_
```

该阶段位于正式 repartition 与镜像写入之前。

当前 QFlash 日志没有进一步展开其闭源 Host 内部算法。设备侧后续 signed image 仍会执行自己的 Secure Verify，NV 也有独立 checksum 机制。

Flash 变化：当前日志未显示该阶段主动修改 Flash。

---

## 2.11 步骤 10：Repartition

QFlash：

```text
15:22:58 _REPARTITION_
```

设备侧 Host command：

```text
BSL_REPARTITION
```

调用链：

```text
dl_cmd_repartition()
    ↓
_parse_repartition_header()
    ↓
dl_repartition()
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_cmd_proc.c
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_nand_operate.c
```

本次日志：

```text
_parse_repartition_header:
version(1),unit(2), table_count(1), table_tag(980181620),
table_offset(16), table_size(2288)
```

随后打印全部 26 个 Volume 的目标配置，并依次找到当前 Volume ID 0～25。

`dl_repartition()` 比较目标表与当前 UBI volume table，源码支持：

| 结果 | 处理 |
|---|---|
| `PART_SAME` | 保持当前 volume layout |
| `PART_DIFF` | 删除 changed / obsolete volume，再创建目标 volume |
| `PART_FAIL` | 擦除 `ubipac`，重新 attach UBI，全量创建 volume |

全量创建路径：

```text
_dl_mtd_part_erase("ubipac")
    ↓
fdl_ubi_dev_init()
    ↓
fdl_ubi_create_vol()
    ↓
fdl_ubi_volume_autoresize()
```

本次日志中的：

```text
volume name:log,size:0x100000,autoresize flag:1
```

对应 `log` 的 autoresize 配置，详见第 4.5 节。

---

## 2.12 步骤 11～39：QFlash 实际下载项完整顺序

下表按本次 QFlash 成功日志原始顺序列出后续全部阶段。

| 步骤 | 时间 | QFlash 阶段 | PAC payload / 控制项 | 设备目标 | 类型 | 实际动作 |
|---:|---|---|---|---|---|---|
| 11 | 15:22:59 | `EraseSPLLOADER` | erase control | `splloader` | raw MTD | 擦除 SPL raw 区 |
| 12 | 15:22:59 | `NV_NR_G` | `nvitem.bin` | NV Group | UBI | 处理 factory/runtime/download NV |
| 13 | 15:23:05 | `EraseUBOOTLOG` | erase control | `uboot_log` ID16 | UBI | `ubi_start_update(..., 0)` 清空 volume |
| 14 | 15:23:05 | `SML` | `sml-sign.bin` | `sml` + `smlbak` | raw MTD | 两个 raw 分区擦除并双写 |
| 15 | 15:23:05 | `Trustos` | `tos-sign.bin` | `trustos` + `trustosbak` | raw MTD | 两个 raw 分区擦除并双写 |
| 16 | 15:23:08 | `Teecfg` | `teecfg-sign.bin` | `teecfg` + `teecfgbak` | raw MTD | 两个 raw 分区擦除并双写 |
| 17 | 15:23:08 | `UBOOTLoader` | `u-boot-sign.bin` | `uboot` + `ubootbak` | raw MTD | 两个 raw 分区擦除并双写 |
| 18 | 15:23:09 | `Sysdump` | `sysdump-sign.bin` | `sysdump` | raw MTD | raw erase/write |
| 19 | 15:23:10 | `EraseDumpstatus` | erase control | `dumpstatus` | raw MTD | 擦除 raw 状态区 |
| 20 | 15:23:10 | `BootLogo` | `quectel_logo.bmp` | `logo` | raw MTD | raw erase/write |
| 21 | 15:23:11 | `BOOT` | `boot-sign.img` | `boot` ID18 | UBI | Secure verify + UBI update |
| 22 | 15:23:14 | `System` | `root-sign.squashfs` | `system` ID19 | UBI | Secure verify + UBI update |
| 23 | 15:23:21 | `Recovery` | `boot-sign.img` | `recovery` ID14 | UBI | Secure verify + UBI update |
| 24 | 15:23:24 | `EraseSysdumpdb` | erase control | `sysdumpdb` ID17 | UBI | 清空 volume |
| 25 | 15:23:24 | `Modem_NR` | signed Modem | `nr_modem` ID10 | UBI | Secure verify + UBI update |
| 26 | 15:23:29 | `Modem_NR_DELTANV` | `deltanv-sign.bin` | `nr_deltanv` ID9 | UBI | UBI update |
| 27 | 15:23:29 | `Modem_NR_PHY` | signed PHY | `nr_phy` ID11 | UBI | Secure verify + UBI update |
| 28 | 15:23:31 | `DSP_LTE_AG` | signed ADSP | `l_agdsp` ID12 | UBI | Secure verify + UBI update |
| 29 | 15:23:33 | `DFS` | signed `UIS8510_SP` | `pm_sys` ID13 | UBI | Secure verify + UBI update |
| 30 | 15:23:33 | `Recoveryfs` | `recovery-sign.squashfs` | `recoveryfs` ID15 | UBI | Secure verify + UBI update |
| 31 | 15:23:35 | `EraseRootfs_data` | erase control | `rootfs_data` ID20 | UBI | 清空 volume |
| 32 | 15:23:35 | `EraseOem_data` | erase control | `oem_data` ID21 | UBI | 清空 volume |
| 33 | 15:23:35 | `Misc_ro` | `misc_ro.ubifs` | `misc_ro` ID22 | UBI | 写入初始 UBIFS image |
| 34 | 15:23:35 | `Misc_rw` | `misc_rw.ubifs` | `misc_rw` ID23 | UBI | 写入初始 UBIFS image |
| 35 | 15:23:35 | `Misc_rw_bak` | `misc_rw.ubifs` | `misc_rw_bak` ID24 | UBI | 写入初始 UBIFS image |
| 36 | 15:23:36 | `EraseLog` | erase control | `log` ID25 | UBI | 清空 autoresize volume |
| 37 | 15:23:36 | `Mcufw` | `Project.bin` | `mcufw` | Host stage | QFlash 执行该阶段；当前 UART 未给出独立 Flash write 锚点 |
| 38 | 15:23:41 | `SPLLoader` | `u-boot-spl-16k-sign.bin` | `splloader` | raw NAND SPL writer | `sprd_nand_write_spl()` |
| 39 | 15:23:41 | `_RESET_` | reset command | 设备 | 控制 | FDL2 Reset |
| 40 | 15:23:41 | `Download Successful.` | — | Host 状态 | — | 本次下载成功 |
| 41 | 15:23:51 | `FW upgrade success` | — | Host 状态 | — | QFlash 完成升级会话 |

以下小节补充上述阶段中的实际设备侧处理。

---

## 2.13 Raw MTD 下载处理

### 2.13.1 目标类型判定

`dl_download_start()` 调用 `_dl_part_info_parse(name)`。

处理顺序：

```text
目标名称
    ↓
_get_mtd_partition_info()
    ├─ 找到 → PART_TYPE_MTD
    └─ 未找到
         ↓
      ubi_open_volume_nm()
         └─ 找到 → PART_TYPE_UBI
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_nand_operate.c
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/parsemtdparts.c
```

`parse_mtd_part_info()` 使用 raw MTD partition table 返回：

```text
partition name
partition offset
partition size
```

成功日志中的：

```text
get mtd part info: "system" is not a mtd partition.
```

表示 raw MTD table 中未找到 `system`，后续转入 UBI Volume lookup。

### 2.13.2 Raw erase

调用链：

```text
BSL_ERASE_FLASH / dl_download_start()
    ↓
dl_cmd_erase() / PART_TYPE_MTD start
    ↓
_dl_mtd_part_erase()
    ↓
nand_erase_opts()
    ↓
mtd_block_isbad()
    ↓
mtd_erase()
```

`_dl_mtd_part_erase()` 使用目标分区的固定 `offset + size` 擦除整个 raw partition。

本次 SML 日志：

```text
Erasing at 0x100000 ... 0x1a0000
Erasing at 0x1c0000 ... 0x260000
```

分别对应：

```text
sml    offset 0x00100000 size 0x000c0000
smlbak offset 0x001c0000 size 0x000c0000
```

### 2.13.3 Raw write

调用链：

```text
BSL_CMD_MIDST_DATA
    ↓
dl_cmd_write_midst()
    ↓
dl_download_midst()
    ↓
_dl_nand_write()
    ↓
dl_nand_write_mtd()
    ↓
part_write_skip_bad()
    ↓
nand_write_skip_bad()
```

本次日志示例：

```text
dl nand write:partname:sml, offset: 0x100000, len: 0x4c698
_dl_nand_write:partname:smlbak,offset:0x1c0000,len:0x4c698
```

同样的主/备写入已在 `trustos`、`teecfg`、`uboot` 阶段出现。

---

## 2.14 Raw 主/备镜像下载

当前 NAND dloader 源码存在 `NAND_UBOOT_DOUBLE_BAKUP` 条件路径。

下载主镜像时：

```text
BSL_CMD_START_DATA(main)
    ↓
dl_cmd_write_start()
    ↓
check_part_with_bak_ab(main, 0)
    ↓
main + "bak"
    ↓
dl_download_start(backup)
    ↓
dl_download_start(main)
```

同一 payload 数据流随后写入主、备两个 raw partition。

当前成功日志实际确认：

```text
SML       → sml + smlbak
Trustos   → trustos + trustosbak
Teecfg    → teecfg + teecfgbak
UBOOT     → uboot + ubootbak
```

当前 target defconfig 未显式列出 `NAND_UBOOT_DOUBLE_BAKUP`，但当前成功 UART 的主/备实际行为与该源码路径一致。

SPL 使用独立 writer，不套用上述普通 raw 主备路径，见第 2.17 节。

---

## 2.15 UBI Volume 下载处理

### 2.15.1 Volume lookup

raw MTD lookup 失败后：

```text
ubi_open_volume_nm(cur_ubi.dev_num, part, UBI_READWRITE)
```

按 Volume name 找到当前 Volume，并设置：

```text
PART_TYPE_UBI
```

本次 `System`：

```text
get mtd part info: "system" is not a mtd partition.
fdl2_download_start:part:system,size:0xee0338
Volume "system" found at volume id 19
```

### 2.15.2 UBI update start

调用链：

```text
dl_download_start()
    ↓
fdl_ubi_volume_start_update(volume, image_size)
    ↓
ubi_start_update()
```

`ubi_start_update()`：

1. 设置 update marker；
2. 对目标 Volume 的现有 LEB 执行 unmap；
3. 为新 image 进入 update 状态；
4. `size == 0` 时完成“清空 Volume”操作并清 marker。

显式 erase item 例如：

```text
EraseRootfs_data
EraseOem_data
EraseLog
EraseUBOOTLOG
EraseSysdumpdb
```

最终均通过 size=0 的 UBI update 语义清空目标 Volume。

### 2.15.3 UBI write

调用链：

```text
dl_download_midst() / dl_download_end()
    ↓
_dl_nand_write()
    ↓
fdl_ubi_volume_write()
    ↓
ubi_more_update_data()
    ↓
write_leb()
    ↓
ubi_eba_write_leb()
    ↓
ubi_io_write_data()
```

写满目标 image 并 flush 后清除 update marker。

本次 `System` 日志：

```text
Volume "system" found at volume id 19
set update marker for volume 19 in uboot.
dl nand write:partname:, offset: 0x0, len: 0xee0338
Volume "system" found at volume id 19
clear update marker for volume 19 in uboot.
```

本次 `Misc_ro`：

```text
Volume "misc_ro" found at volume id 22
set update marker for volume 22 in uboot.
fdl2_download_start:part:misc_ro,size:0x193000
dl nand write:partname:, offset: 0x0, len: 0x193000
clear update marker for volume 22 in uboot.
```

---

## 2.16 `NV_NR_G` Group NV 写入

当前：

```text
CONFIG_NV_SUPPORT_GROUP=y
```

`NV_NR_G` payload 为：

```text
nvitem.bin
```

入口目标：

```text
nr_downloadnv
```

完整设备侧主链：

```text
nr_downloadnv image
    ↓
dl_nand_write_nv_img()
    ↓
dl_nand_write_grpnv_img()
    ├─ nand_write_factorynv()
    │    ├─ nr_factorynv
    │    └─ calinv
    ├─ nand_write_runtimenv()
    │    ├─ nr_runtimenv1
    │    └─ nr_runtimenv2
    └─ nand_write_nvpart()
         └─ nr_downloadnv
```

本次日志对应出现：

```text
calinv
nr_factorynv
nr_runtimenv2
nr_runtimenv1
nr_downloadnv
```

NV 数据在写入前还会比较 Host write-start 提供的 checksum 与设备接收到的 NV 数据 checksum。

---

## 2.17 `SPLLoader` 特殊写入

`SPLLoader` 位于 QFlash 下载项末尾：

```text
15:23:41 SPLLoader
```

PAC payload：

```text
u-boot-spl-16k-sign.bin
```

FDL2 对 `splloader` 使用特殊分支：

```text
_dl_nand_write()
    ↓
sprd_nand_write_spl(buffer, mtd, length)
    ↓
write spl pass!
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_nand_operate.c
```

当前成功日志：

```text
fdl2_download_start:part:splloader,size:0x33eb8
sprd_fb_secure_verify(): name: splloader0 enter
...
dl nand write:partname:splloader, offset: 0x0, len: 0x33eb8
write spl pass!
```

`splloaderbak` 的底层地址选择逻辑未在当前已定位的 `sprd_nand_write_spl()` 调用边界中展开，因此本文不将普通 `main + bak` raw 双写路径套用于 SPL。

---

## 2.18 Reset 与下载结束

QFlash：

```text
15:23:41 _RESET_
15:23:41 Download Successful.
15:23:41 Reset...
15:23:51 FW upgrade success
```

设备接收：

```text
BSL_CMD_NORMAL_RESET
```

对应：

```text
dl_cmd_reboot()
```

Reset 后设备离开下载态，重新从 NAND 启动。

---

# 3. 烧录完成后的正常启动

本章仅描述 2026-09-07 成功烧录后的正常启动闭环。

## 3.1 Reset 后重新初始化 DDR 与 NAND

Reset 后串口重新出现 DDR training：

```text
ddr init start!!!
...
set freq:1536MHz
ddr init pass!!!
Enter nand_boot
PHASE_FLASH_INIT
Read ID:
0XC2 0XA6 0X3 0X0
```

当前 NAND：

```text
MXIC MX35UF2GE4AD
ID C2 A6 03
256 MiB
```

---

## 3.2 早期 Secure Boot Chain

早期启动依次验证：

```text
PHASE_VERIFY_TEE
PHASE_VERIFY_SML
PHASE_VERIFY_TOS
PHASE_LOAD_BOOTLOADER
```

日志均出现：

```text
Checking...
verifed success
```

随后：

```text
Jump to SML
```

并进入：

```text
U-Boot 2023.01
Model: Spreadtrum uis8510_1h10_nand Board
DRAM: 441 MiB (effective 512 MiB)
```

---

## 3.3 U-Boot 识别 NAND 与 UBI

U-Boot：

```text
nand id is c2 a6 3 0
256 MiB
ubi init start
boot NORMAL
```

成功现场 UBI：

```text
ubi0: attached ... (name "ubipac", size 232 MiB)
ubi0: PEB size: 131072 bytes
ubi0: LEB size: 126976 bytes
ubi0: good PEBs: 1862
ubi0: bad PEBs: 0
ubi0: user volume: 26
ubi0: PEBs reserved for bad PEB handling: 40
```

随后 U-Boot 打印完整 raw partition table，包括：

```text
splloader
splloaderbak
sml
smlbak
trustos
trustosbak
teecfg
teecfgbak
sysdump
dumpstatus
uboot
ubootbak
rawdata
custinfo
logo
ubipac
```

---

## 3.4 Normal boot 使用 `boot` 与 `system`

U-Boot normal boot：

```text
partition = boot
```

当前 Kernel cmdline：

```text
init=/sbin/init
ubi.block=0,19
root=/dev/ubiblock0_19
rootfstype=squashfs
ro
rootwait
```

当前 `system` 为 UBI Volume ID19，BBA `root-sign.squashfs` 烧入该 Volume 后作为 Linux 只读根文件系统。

---

## 3.5 BBA 用户态启动

成功日志：

```text
starting pid 275, tty '': '/etc/init.d/rcS'
```

当前 BBA 主线：

```text
/sbin/init
    ↓
/etc/inittab
    ↓
/etc/init.d/rcS
    ↓
/etc/init.d/rcS.quecopen_rg620ua
```

supplier rcS 成功挂载：

```text
rootfs_data → /rootfs_data
oem_data    → /oem_data
log         → /log
prodnv      → /etc/productinfo
misc_ro     → /var/run/misc/misc_ro
misc_rw     → /var/run/misc/misc_rw
misc_rw_bak → /var/run/misc/misc_rw_bak
```

日志：

```text
rcS.quecopen_rg620ua: mounted rootfs_data -> /rootfs_data
rcS.quecopen_rg620ua: mounted oem_data -> /oem_data
rcS.quecopen_rg620ua: mounted log -> /log
rcS.quecopen_rg620ua: mounted prodnv -> /etc/productinfo
rcS.quecopen_rg620ua: mounted misc_ro -> /var/run/misc/misc_ro
rcS.quecopen_rg620ua: mounted misc_rw -> /var/run/misc/misc_rw
rcS.quecopen_rg620ua: mounted misc_rw_bak -> /var/run/misc/misc_rw_bak
rcS.quecopen_rg620ua: ubivol_link done
...
rcS.sdk init done!
```

---

# 4. 补充机制与边界

## 4.1 UBI Update Marker

每次 UBI Volume update 开始：

```text
set_update_marker()
```

将该 Volume 的 `upd_marker` 写入 UBI volume table。

随后：

```text
ubi_start_update()
    ↓
对旧 LEB 执行 unmap
    ↓
写入新 image
    ↓
ubi_wl_flush()
    ↓
clear_update_marker()
```

`size == 0` 的 erase item 在清空 Volume 后直接 flush 并清 marker。

读取路径会拒绝 update marker 仍保持置位的损坏 Volume。

主要源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/upd.c
```

---

## 4.2 Raw 主备写入

当前成功现场实际双写：

```text
sml       + smlbak
trustos   + trustosbak
teecfg    + teecfgbak
uboot     + ubootbak
```

源码路径：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_cmd_proc.c
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_nand_operate.c
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/parsemtdparts.c
```

普通 backup 名使用：

```text
<main> + "bak"
```

例如：

```text
sml → smlbak
uboot → ubootbak
```

---

## 4.3 NV Group

当前：

```text
CONFIG_NV_SUPPORT_GROUP=y
```

主要配置：

```text
nr_factorynv
nr_runtimenv1 ↔ nr_runtimenv2
nr_downloadnv
```

`nr_downloadnv` image 在全刷时负责重建：

```text
nr_factorynv
calinv
nr_runtimenv1
nr_runtimenv2
nr_downloadnv
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_download_nv.c
```

---

## 4.4 NAND Bad Block

### 4.4.1 Raw erase

`nand_erase_opts()` 每个 eraseblock 先调用：

```text
mtd_block_isbad()
```

已标坏块直接跳过。

### 4.4.2 Raw write

`part_write_skip_bad()` 跳过已标坏块。

写失败时 FDL2 可调用：

```text
_block_markbad()
```

将当前 eraseblock 标坏。

### 4.4.3 UBI

UBI attach：

```text
scan_peb()
    ↓
ubi_io_is_bad()
    ↓
mtd_block_isbad()
```

UBI 写 PEB 遇 `-EIO` 时：

```text
ubi_eba_write_leb()
    ↓
recover_peb()
```

wear-leveling 擦除发现新坏块时会执行：

```text
ubi_io_mark_bad()
```

并消耗 bad-PEB reserve。

主要源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/nand/raw/nand_util.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/nand/raw/sprd_nand_util.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/attach.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/io.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/eba.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/wl.c
```

---

## 4.5 `log` autoresize

当前 XML：

```text
Partition id=log size=0xFFFFFFFF
```

FDL2 定义：

```c
#define AUTO_RESIZE_FLAG 0xFFFFFFFF
```

解析流程：

```text
size == 0xFFFFFFFF
    ↓
vtbl.size = 1 MiB
vtbl.autoresize = 1
    ↓
fdl_ubi_create_vol(..., 1 MiB)
    ↓
记录 auto_resize_id
    ↓
所有固定 Volume 创建完成
    ↓
fdl_ubi_volume_autoresize()
    ↓
使用剩余 avail_pebs 扩容 log
```

成功日志：

```text
volume name:log,size:0x100000,autoresize flag:1
```

源码：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_nand_operate.c
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/dloader/dl_ubi.c
```

---

## 4.6 Verify / Integrity Check

当前能够直接确认的校验：

1. QFlash 在 repartition 前执行 `_INTEGRITY_CHECK_`；
2. 本次 UART 对多个 signed image 输出 `sprd_fb_secure_verify()` / `uboot_verify_img`；
3. NV Group 在写入前比较 Host 提供的 checksum 与设备接收数据 checksum；
4. UBI update 路径存在 `ubi_check_volume()`；
5. FDL2 源码存在 `dl_download_flush_data()` 的 SHA-256 buffer 校验实现。

当前 raw MTD 普通写入直接调用链中没有确认“整分区写后再做全量 readback checksum”的固定步骤。

QFlash Host 是否额外调用 READ_FLASH handler 做整分区 readback verify，不在当前开放源码与 QFlash摘要日志的直接证据范围内。

---

## 4.7 `ubipac` 的 U-Boot / Linux MTD 编号

成功 U-Boot 日志出现：

```text
ubi0: attaching mtd3
ubi0: attached mtd3 (name "ubipac", size 232 MiB)
```

同一启动日志打印整颗 NAND raw partition table 时，`ubipac` 位于第 16 个分区；Linux cmdline 也以完整 raw MTD 布局描述 `ubipac`。

U-Boot 下载/启动环境内部用于 UBI attach 的 MTD device index 与 Linux `/proc/mtd` 的编号空间不同。本文在分区映射中使用 partition name `ubipac`，Linux 侧使用 `mtd15` 语义。

---

## 4.8 final `pac.ini` 中未形成独立 QFlash 阶段的控制项

当前 final `pac.ini` 包含：

```text
FLASH_NR
EraseUBOOT
EraseUserData
```

本次 QFlash 成功摘要没有对应的独立同名阶段。

本文只将实际 QFlash / UART 已对应的阶段写入主流程，不根据条目名称额外增加一次未观测到的 Flash 操作。

---

## 4.9 当前闭源边界

以下内部细节不作为本文主链结论：

- QFlash 打开 PAC、解析 PAC binary container 的内部实现；
- Host 如何把 XML partition table 转换成 BSL repartition binary payload；
- `NvBackup=1` 在 QFlash Host 侧的完整内部策略；
- `sprd_nand_write_spl()` 内部如何布局 SPL primary / backup boot area；
- QFlash 是否额外执行完整 raw partition readback verify；
- `Mcufw` 阶段在当前提供 UART 中的具体 Flash writer。

---

# 5. 关键源码索引

## 5.1 PAC 构建

| 内容 | 路径 / 函数 |
|---|---|
| BBA image / RootFS / PAC 接入 | `platform/build/makes/Makefile.sdk.quecopen_rg620ua.mk` |
| Model packaging 配置 | `platform/build/model/M8280/EU/exfiles/ql_build_config` |
| SDK 执行副本 | `sdk/ql_rg620ua/quectel/unisoc/compile/ql_build_config` |
| Model Partitions.ini | `platform/build/model/M8280/EU/partitions/Partitions.ini` |
| Model Partitions.xml | `platform/build/model/M8280/EU/partitions/Partitions.xml` |
| `build_fw_pack()` | `ql_build_config` |
| `build_sign()` | `ql_build_config` |
| `build_pac()` | `ql_build_config` |
| CP 签名 | `sdk/ql_rg620ua/unisoc/prebuilts/scripts/packimage_scripts/sign_cp.sh` |
| PAC Workspace | `sdk/ql_rg620ua/bin/unisoc/temp/` |

## 5.2 FDL2 下载

| 内容 | 路径 / 函数 |
|---|---|
| Download command 入口 | `.../dloader/cmd_download.c::do_download()` |
| FDL2 prepare | `.../dloader/cmd_download.c::dl_prepare_init()` |
| Handler 注册 | `.../dloader/cmd_download.c::dl_register_func()` |
| Command loop | `.../dloader/cmd_download.c::dl_cmd_handler()` |
| Write / erase / repartition command | `.../dloader/dl_cmd_proc.c` |
| NAND / UBI type dispatch | `.../dloader/dl_nand_operate.c` |
| MTD partition parser | `.../dloader/parsemtdparts.c` |
| UBI dloader wrapper | `.../dloader/dl_ubi.c` |
| NV Group | `.../dloader/dl_download_nv.c` |

完整前缀：

```text
sdk/ql_rg620ua/unisoc/uboot44/arch/arm/mach-sprd/
```

## 5.3 NAND / UBI 底层

```text
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/nand/raw/nand_util.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/nand/raw/sprd_nand_util.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/upd.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/attach.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/io.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/eba.c
sdk/ql_rg620ua/unisoc/uboot44/drivers/mtd/ubi/wl.c
```

---

# 6. 排障索引

## 6.1 无法从正常系统切入下载模式

优先检查 QFlash 是否依次出现：

```text
Port is AT port
AT+QDOWNLOAD=1
AT Port not exist[PASS]
SPRD U2S DIAG
QDownload Success
```

缺失位置即为当前切换链的故障边界。

## 6.2 卡在 FDL / FDL2

Host：

```text
DL Start
FDL
FDL2
```

设备侧应进入：

```text
do_download
dl_prepare_init
dl_cmd_handler
```

如果只到 FDL 不到 FDL2，先排下载阶段 Loader；如果 FDL2 已进入但无 command，再排 Host command / packet 交互。

## 6.3 卡在 Repartition

检查：

```text
_INTEGRITY_CHECK_
_REPARTITION_
_parse_repartition_header
volume name:...
```

源码入口：

```text
dl_cmd_repartition()
_parse_repartition_header()
dl_repartition()
```

重点检查当前 XML / repartition table 是否与目标 UBI 容量一致。

## 6.4 Raw MTD 写失败

关键日志：

```text
Erasing at ...
dl nand write:partname:...
nand write error ... mark bad block ...
```

重点检查：

```text
parse_mtd_part_info()
_dl_mtd_part_erase()
part_write_skip_bad()
```

## 6.5 UBI Volume 写失败

关键日志：

```text
"xxx" is not a mtd partition
Volume "xxx" found at volume id N
set update marker
clear update marker
```

重点检查：

```text
fdl_ubi_dev_init()
fdl_ubi_volume_start_update()
fdl_ubi_volume_write()
ubi_start_update()
```

## 6.6 下载成功但无法正常启动

先区分：

1. QFlash 是否已经出现：

```text
Download Successful.
FW upgrade success
```

2. Reset 后是否出现：

```text
Enter nand_boot
PHASE_VERIFY_TEE
PHASE_VERIFY_SML
PHASE_VERIFY_TOS
PHASE_LOAD_BOOTLOADER
U-Boot 2023.01
```

3. U-Boot 是否 attach `ubipac`：

```text
ubi init start
attached ... name "ubipac"
user volume: 26
```

4. Kernel cmdline 是否包含：

```text
init=/sbin/init
ubi.block=0,19
root=/dev/ubiblock0_19
```

5. BBA 用户态是否进入：

```text
/etc/init.d/rcS
rcS.quecopen_rg620ua
rcS.sdk init done!
```

---

# 7. 全链路索引

```text
BBA filesystem
    ↓
mksquashfs4
    ↓
BBA root.squashfs
    ↓
build_fw_pack()
    ↓
bin/unisoc/temp 物料汇聚
    ↓
AP / Boot signing
    ↓
Partitions.ini → parser_ini.py → pac.ini
    ↓
CP signing
    ↓
binary_gzip
    ↓
makepac.py
    ↓
最终 PAC

QFlash
    ↓
查找 Quectel USB AT Port
    ↓
AT+QDOWNLOAD=1
    ↓
AT Port 消失
    ↓
SPRD U2S DIAG / QDLoader 枚举
    ↓
FDL1
    ↓
FDL2
    ↓
_BKF_NV_NR_G
    ↓
_INTEGRITY_CHECK_
    ↓
_REPARTITION_
    ↓
FDL2 按名称判定 raw MTD / UBI Volume
    ↓
逐项 Erase / Write
    ↓
SPLLoader
    ↓
_RESET_
    ↓
Download Successful
    ↓
Reset 后重新进入 NAND Boot Chain
    ↓
Secure verify
    ↓
U-Boot
    ↓
attach ubipac
    ↓
boot ID18 + system ID19
    ↓
Linux /sbin/init
    ↓
rcS / rcS.quecopen_rg620ua
    ↓
BBA Userspace
```
